<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lqs.mall.dao.UmsRoleDao">
    <select id="getMenuList" resultType="com.lqs.mall.model.UmsMenu">
        select
            m.id id,
            m.parent_id parentId,
            m.create_time createTime,
            m.title title,
            m.level level,
            m.sort sort,
            m.name name,
            m.icon icon,
            m.hidden hidden
        from
            ums_admin_role_relation arr
            left join ums_role r on r.id = arr.role_id
            left join ums_role_menu_relation rmr on r.id = rmr.role_id
            left join ums_menu m on m.id = rmr.menu_id
        where arr.admin_id = #{adminId}
        and m.id is not null
        group by
            m.id
    </select>
</mapper>